

struct Point // structures hold data
{
    double x;
    double y;
};

double euclidean_dist(struct Point p1, struct Point p2){
    // takes in two structs and assigns local variable p1, p2
    // p1.x p1.y - looks into struct 1 and takes .x and .y, each struct has and x and y var
};

int main()
{

    // passing struct to function copies over every element

    return 0;
}