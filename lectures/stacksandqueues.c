#include <stdio.h>

int main()
{
    // STACK
    // last in first out, stuff added to top of stack, top is removed first
    // OPERATIONS:
    // peek, gets value of top of stack
    // pop, remove top of stack
    // push, add element to top of stack

    // QUEUE
    //  first in first out, stuff added to back
    //  peek, read first element in front
    //  dequeue, remove element at the front of queue
    //  enqueue, add element to back of queue

    // runtime depends on implementation
    // uses linked lists or arrays

    return 0;
}